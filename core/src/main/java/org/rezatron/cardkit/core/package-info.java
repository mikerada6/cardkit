/**
 * CardKit core: immutable card primitives, deck/shoe, randomness seam, hand, and participants.
 *
 * <p>This module holds no game rules and depends only on the JDK and the SLF4J logging facade.
 * Concrete types land in stories 0003 onward; this package-info keeps the module compiling while
 * the build skeleton is proven (story 0001).</p>
 */
package org.rezatron.cardkit.core;
