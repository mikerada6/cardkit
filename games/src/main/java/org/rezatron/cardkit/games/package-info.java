/**
 * CardKit games: game-specific rules (poker, blackjack), participants, and session state.
 *
 * <p>This module depends on {@code cardkit-core} and composes its primitives with game rules.
 * Concrete types land in later stories; this package-info keeps the module compiling while the
 * build skeleton is proven (story 0001).</p>
 */
package org.rezatron.cardkit.games;
